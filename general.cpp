
#include "general.h"

void readInfile()
{
    // TODO: sets appropriate var to input, and correctly formatted
}

bool check_numbers(char **numbers, int count)
{
    for (int i = 0; i < count; ++i)
    {
    }
}
