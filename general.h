#include <iostream>
#include <cstring>
#include <vector>
using namespace std;

// setup general/shared variables
char **numbers;
int count;

void readInfile();

bool check_numbers(char **numbers, int count);
